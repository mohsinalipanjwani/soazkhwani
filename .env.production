# Production build config for the frontend (loaded automatically by `vite build`).
# This is PUBLIC config, not a secret — it's just the URL of the API Worker, which
# is already public. Committing it means the deployed bundle always points at the
# right API without relying on a dashboard build variable.
VITE_API_BASE=https://noha-directory-api.nohalyrics.workers.dev
